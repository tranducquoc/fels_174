<%= form_for [:admin, @category] do |f| %>
  <%= render "/shared/error_messages", object: f.object %>
  <%= f.label :name %>
  <%= f.text_field :name, class: "form-control" %>
  <%= f.submit yield(:button_title), class: "btn btn-primary" %>
<% end %>
